import React from 'react'
import styled from 'styled-components'

const LogoText = styled.h1`
  font-family: 'Dancing Script', cursive;
  font-size: 3rem;
`

const logoText = () => (
  <>
    <LogoText>Miloah</LogoText>
  </>
)

export default logoText
